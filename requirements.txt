# Зависимости проекта «DeepSeek Chat (веб-интерфейс)».
#
# ЯДРО проекта (чат, агент, модели, память, стратегии, задачи) намеренно
# построено только на стандартной библиотеке Python 3.7+ (http.server,
# urllib, json, re, threading, mimetypes, pathlib и т.п.) — без сторонних
# пакетов.
#
# ЕДИНСТВЕННАЯ внешняя зависимость — пакет `mcp` (Model Context Protocol):
# он нужен ТОЛЬКО для MCP-слоя (подключение к MCP-серверу, list_tools/
# call_tool, см. rtk_app/mcp_client.py, client.py, test_server.py).
# Зависимость ОПЦИОНАЛЬНАЯ: веб-сервер проекта запускается и без неё — тогда
# статус MCP в интерфейсе будет «недоступен (не установлен пакет mcp)».
#
# Требуется Python >= 3.9 (для пакета mcp).
#
# Установка:  pip install -r requirements.txt
mcp


