# Зависимости проекта «DeepSeek Chat (веб-интерфейс)».
#
# Проект НАМЕРЕННО не использует сторонние библиотеки: всё построено только
# на стандартной библиотеке Python 3.7+ (http.server, urllib, json, re,
# threading, mimetypes, pathlib и т.п.). Поэтому внешних пакетов нет —
# установка `pip install -r requirements.txt` ничего не делает, но файл
# фиксирует это явно (и служит точкой для будущих зависимостей).
#
# Требуется только Python >= 3.7.
#
# (Например, если позже понадобится подключить локальную копию KaTeX или
#  сторонний HTTP-клиент — сюда добавляются соответствующие строки:
#     katex-local==x.y.z
# )
